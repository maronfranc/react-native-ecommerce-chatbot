import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  chatMessage: {
    width: "100%",
    marginBottom: 5,
    padding: 10,
    backgroundColor: "#eee",
    flexDirection: "row",
    alignItems: "center"
  },
});
export default styles;