import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  image: {
    maxHeight: '75%',
    width: '100%'
  },
  buttonContainer: {
    flexDirection: 'row',
    margin: 50,
  }
});

export default styles;